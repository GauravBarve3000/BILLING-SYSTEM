DROP DATABASE Billing;

CREATE DATABASE Billing;

USE Billing;

CREATE TABLE USERS(
	UID INT AUTO_INCREMENT,
	Num VARCHAR(100),
	Name  VARCHAR(100),
	Email VARCHAR(100),
	Total INT,
    PRIMARY KEY (UID)
);
